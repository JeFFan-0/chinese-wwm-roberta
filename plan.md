# 中文 RoBERTa 逐层分析计划

> 项目目录：`/home/intern_fjq_2026/Projects/chinese-wwm-roberta/`
> 状态：**阶段一/二/三 主体已完成**（`check.ipynb`），阶段四/五 待实施
> 环境：conda env `nlp_fjq`（Python 3.10，torch + transformers），Jupyter kernel `nlp_fjq`，机器有 CUDA（cuda:0）

---

## 1. 项目概览

### 1.1 背景

我们手上有两个模型：
- **微调模型**：`chinese-wwm-roberta.ckpt`（409 MB，102.3 M 参数，含自定义 `fc` 分类头，2 分类）
- **原模型底座**：`chinese-roberta-wwm-ext/`（本地下载的官方预训练权重）

两者同架构同词表（vocab=21128，12 层，hidden=768，12 head，intermediate=3072），因此可以**逐层对齐比较**。

### 1.2 五项目标

| # | 目标 | 一句话解释 | 当前状态 |
|---|------|-----------|---------|
| ① | 分析微调模型每一层 | 看自己 ckpt 每层权重 + 激活 | ✅ 已实现 |
| ② | 分析原模型每一层 | 同样的分析作用于底座 | ✅ 已实现 |
| ③ | 对比 finetune 前后每层输出 | 定位微调动了哪些层 | ✅ 已实现 |
| ④ | 每层在激活什么 / 让每层输出有价值 | 探针、注意力、层级分工，摆脱"只用最后一层" | ⬜ 待实施 |
| ⑤ | 每层加模型头 | early-exit / 分层头架构 | ⬜ 待实施 |

### 1.3 已建立的关键事实（阶段三结论）

微调对 backbone 的改动**极小**：
- 199 个 `bert.*` 权重与底座**没有一个完全相等**，但最大绝对误差仅约 **1.5e-3**
- 逐层权重差异均匀（rel_l2 ≈ 0.002），但**激活差异随层深单调放大**：余弦相似度 layer0=0.99998 → layer12=0.980，L2 距离 0.09 → 4.25
- 改动相对最大的模块：`pooler`、layer 8–10 的 query/key、以及 **`fc` 分类头**（[2,768]）

含义：**④ 和 ⑤ 比单纯对比更有产出价值**——既然 backbone 几乎没动，模型行为基本继承底座，"让每层输出有用"的关键在于探针与分层头。

---

## 2. 现有资产与约定

### 2.1 文件

| 路径 | 说明 |
|------|------|
| `chinese-wwm-roberta.ckpt` | 微调权重（目标对象） |
| `chinese-roberta-wwm-ext/` | 本地底座（config.json / pytorch_model.bin / vocab.txt / tokenizer 全套） |
| `check.ipynb` | 主分析工作簿（31 cell，已执行） |
| `plan.md` | 本计划 |

### 2.2 固定约定

- 模型加载一律走**本地底座目录**（不联网）；在线下载经镜像 `HF_ENDPOINT=https://hf-mirror.com`
- `AutoModel.from_config(config, attn_implementation="eager")` + 手灌 ckpt 权重（剥 `bert.` 前缀）＝ 微调模型
- `AutoModel.from_pretrained(BASE, attn_implementation="eager")` ＝ 原模型
- 对比时两个模型**必须用同一 tokenizer、同一输入**，hidden state 才对得齐
- 绘图先启用 `WenQuanYi Micro Hei` 中文字体

---

## 3. 总体技术路线

```
      ┌──────────────┐   同一输入     ┌──────────────┐
      │  微调模型     │ ───────────►  │  原模型       │
      │ (ckpt权重)   │                │ (底座权重)    │
      └──────────────┘                └──────────────┘
              │  每层 hidden / attention 都抓出来
              ▼
   ┌─────────────────────────────────────────┐
   │ ① 权重统计 · ② 每层激活 · ③ 前后对比       │ ← 已完成
   └─────────────────────────────────────────┘
              ▼ 输出"层级分工"假设
   ┌─────────────────────────────────────────┐
   │ ④ 探针 / 注意力 / CKA：验证每层在做什么    │ ← 阶段四
   └─────────────────────────────────────────┘
              ▼ 选出"哪几层够用"
   ┌─────────────────────────────────────────┐
   │ ⑤ 分层头（early-exit）：每层接分类头       │ ← 阶段五
   └─────────────────────────────────────────┘
```

---

## 4. 阶段一：分析微调模型的每一层

### 4.1 目标
回答：**"这个 ckpt 每层长什么样、层与层之间有什么规律"**。分静态（权重）与动态（激活）两个视角。

### 4.2 已有实现（check.ipynb）
- 权重统计表：每张权重张量的 shape / numel / mean / std / absmean / min / max / l2norm
- 按 head 拆解 attention：每层每 head 的 q/k/v 范数、找出异常（退化）head
- 相邻层对应权重的余弦相似度（层间差异）
- query 矩阵奇异值谱（有效秩）
- 逐层激活统计：CLS 范数 / mean_abs / std / max_abs

### 4.3 待补充（可选增强）
- [ ] 权重分布的**层间熵/峰度**演化
- [ ] FFN 的**死神经元比例**（ReLU 后恒为 0 的维度）
- [ ] 逐层 hidden state 的**有效秩**（SVD 谱衰减斜率）
- [ ] 多句子的激活统计（目前只有 2 句，建议 ≥50 句取平均，统计才稳）

### 4.4 交付物
- 每层一张"体检表"（DataFrame）
- 异常 head / 死神经元清单

### 4.5 验收标准
- 每层都能回答：模块数、参数量、权重分布、异常单元
- 统计基于足够样本（≥50 句）时结果稳定

---

## 5. 阶段二：分析原模型每一层

### 5.1 目标
与阶段一**完全对称**地分析底座。

### 5.2 做法
- 复用阶段一同一套代码，模型换成 `base`（见 2.2 约定）
- 输出同样结构的两张表，直接与阶段一对照

### 5.3 交付物 / 验收标准
- 一套可复用分析函数（输入模型 → 输出该模型逐层体检表）
- 底座与微调模型各自独立的逐层档案

---

## 6. 阶段三：对比 finetune 前后每层输出

### 6.1 目标
回答：**"微调到底动了哪些层？动的模式是什么？"**

### 6.2 已有实现
- **激活级**：逐层余弦相似度 / 平均 L2 距离 / 平均绝对差；注意力矩阵余弦
- **权重级**：高精度 cos（6 位小数）+ `mean_abs_diff` + `rel_l2`（不能只用 4 位小数的 cos，1e-3 级差异会被舍成 1.0）
- 双面板图：激活变化 + 权重变化并排

### 6.3 待补充（增强可信度）
- [ ] **多文本语料**做平均（当前 2 句，样本太少）
- [ ] **逐层 CKA**（Centered Kernel Alignment）代替逐 token 余弦，衡量表征整体对齐度：
  ```
  CKA(X,Y) = HSIC(X,Y) / sqrt(HSIC(X,X)·HSIC(Y,Y))
  ```
- [ ] 各层差异的**相对倍数**（layer12/layer0 放大多少倍）
- [ ] 对同一输入做 **position 级差异热图**（看哪个位置被改动大）

### 6.4 交付物
- 一张"微调影响曲线"（层 × 指标）
- 结论文字：哪些层/哪些模块被重点改动

### 6.5 验收标准
- 结论在 ≥50 句语料上稳定复现
- 能明确回答：高层 vs 低层、pooler vs encoder、哪些 proj 改动最大

---

## 7. 阶段四：每层在激活什么 / 让每层输出有价值

> 核心思路：**逐层"探针"**。在每一层的 hidden state 上挂一个小分类头，看哪一层包含的**任务信息**最多，从而把每层当作可用的特征源，而不只是吃最后一层。

### 7.1 线性探针（Linear Probing）—— 主方法
1. 固定模型，冻结参数
2. 对每层 `hidden_states[l][:, 0]`（CLS）挂一个 `Linear(768 → n_labels)`，单独训 3–5 epoch
3. 记录每层在验证集上的准确率 → 得到 **层 × 准确率** 曲线
4. 曲线形状解读：
   - 低层就很高 → 任务简单，浅层特征就够
   - 高层才陡升 → 任务语义集中在深层
   - 层与层之间的"平台/陡升"位置 → 层级分工的分界

**实现骨架**：
```python
def probe(layer_idx, train_ds, val_ds, epochs=3):
    head = nn.Linear(768, n_labels).to(DEVICE)
    opt = torch.optim.Adam(head.parameters(), lr=3e-3)
    for _ in range(epochs):
        for ids, mask, y in DataLoader(train_ds, batch_size=16, shuffle=True):
            h = model(ids, mask, output_hidden_states=True).hidden_states[layer_idx][:, 0]
            opt.zero_grad(); loss = F.cross_entropy(head(h), y); loss.backward(); opt.step()
    # 在验证集上算准确率返回
```

### 7.2 注意力头分工分析
- 对每层每 head 统计：对 `[CLS]` 的关注、对 `[SEP]` 的关注、自注意（对角线）占比、注意力熵
- 识别：CLS 头、句法头、弥散头（高熵）
- 跨层看 head 角色是否变化（低层句法 → 高层语义）

### 7.3 激活稀疏性与死神经元
- 每层 FFN 中间激活中恒为 0 的比例（ReLU 死区）
- 死神经元越多的层往往越"压缩/高抽象"

### 7.4 层间表征对齐（CKA）
- **同模型内**：各层 hidden state 两两 CKA → 层级冗余度热图（哪些层高度相似、可以跳）
- 结果直接支撑阶段五（相似层可共用一个头）

### 7.5 信息流分析（进阶，可选）
- 层间互信息估计（对激活做 bin 化后算 MI）
- 或用可解释性工具（如 attention 归因、集成梯度）看某层对最终预测的贡献

### 7.6 阶段四 → 阶段五的联动
探针结果直接回答两个设计问题：
1. **哪些层值得挂头**？（准确率已经到瓶颈的层没必要再挂）
2. **任务信息在第几层饱和**？（决定 early-exit 的最浅可用层）

### 7.7 交付物
- 探针精度曲线（层 × acc）
- 注意力 head 分工表
- CKA 冗余度热图
- 一份"层级分工"结论（哪几层分别负责什么）

### 7.8 验收标准
- 每条结论都有量化支撑（曲线/热图/表）
- 明确给出"任务信息在第几层饱和"的数字结论

---

## 8. 阶段五：给每层加模型头（early-exit / 分层头）

### 8.1 架构
在每个 hidden state 上各挂一个分类头（embedding 层 + 12 个 encoder 层 = 13 个头）：

```python
class LayerHeads(nn.Module):
    def __init__(self, bert, n_labels, hidden=768, n_layers=12):
        super().__init__()
        self.bert = bert
        self.heads = nn.ModuleList([nn.Linear(hidden, n_labels)
                                    for _ in range(n_layers + 1)])
        for p in self.bert.parameters():          # 默认冻结 backbone
            p.requires_grad = False

    def forward(self, input_ids, attention_mask=None):
        out = self.bert(input_ids, attention_mask=attention_mask,
                        output_hidden_states=True)
        return [head(h[:, 0]) for h in out.hidden_states]   # 每层 CLS → logits
```

可选：把 ckpt 里的 `fc` 头（[2,768]）初始化最深层头（`heads[-1]`），冷启动更好。

### 8.2 训练策略（三选一，按实验目的）

| 策略 | 做法 | 适用目的 |
|------|------|---------|
| A. 逐头独立 | 每个头单独在对应层激活上训 | 复现/验证阶段四探针结论 |
| B. 联合训练（推荐） | 所有头一起训，loss = Σ 各头 CE | early-exit 主路径，各层头互相正则 |
| C. 加权联合 | 浅层头损失给更高权重 | 重点强化浅层可用性 |

初始**冻结 backbone** 跑通；之后可尝试解锁深层（layer 8–12）微调 1–2 epoch，观察是否提升。

### 8.3 早停推理
- 对每个样本，从浅层开始，当某层头的预测**置信度（softmax 最大概率）≥ 阈值** 或 **熵 ≤ 阈值** 时提前退出
- 统计：达到阈值时通常停在第几层 → 计算**平均推理层数（加速比）**

### 8.4 评估指标
- **每层头单独**在验证集上的准确率（应大致等于阶段四探针曲线，互相印证）
- **early-exit 系统**：准确率 vs 平均层数（加速比）权衡曲线
- 与只用最后一层的 baseline（就是 ckpt 自带 `fc` 头）对比：**相同准确率下省多少层**

### 8.5 与阶段四联动
- 阶段四 CKA 显示高度相似的相邻层 → 可以在这些层**共享一个头**（减少参数）
- 阶段四探针显示信息已饱和的层 → early-exit 阈值可设在此层附近

### 8.6 交付物
- 训练好的 `LayerHeads` 模型 + 每层头精度表
- early-exit 精度-延迟曲线图
- 保存模型文件（`.pt`）

### 8.7 验收标准
- 每层头精度曲线与探针结论一致
- early-exit 在**精度损失 ≤1–2 个百分点**的前提下达到可量化的平均层数下降（目标：平均 ≤6 层）
- 复现性：固定 seed 可复跑

---

## 9. 数据需求

阶段四和阶段五**必须要有带标签的分类数据**（fc 头是 2 分类，推测你的任务可能是二分类，如情感分析 / 二分类文本判断）。请确认：

| 项 | 说明 |
|----|------|
| 任务类型 | 二分类？多分类？ |
| 数据格式 | 期望 `(text, label)` 列表 / CSV / 其它 |
| 数据量 | 训练/验证各多少条 |
| 数据位置 | 是否已有本地文件 |

### 无现成数据时的方案（任选）
- **公开中文数据集**（经镜像下载，直接联网不通）：
  - 情感二分类：`lansinuote/ChnSentiCorp`、`seamew/ChnSentiCorp`（HF 镜像）
  - 多分类：`THUCNews` 子集（分类、体育、娱乐、财经、房产、教育、科技、社会、时政、游戏）
  - 句子对任务：`LCQMC`
- **自制小集**：先收集 200–500 条真实文本人工打标，够探针和冒烟训练用
- **完全离线兜底**：用已有 ckpt 对未标注文本预测，用预测结果当伪标签做机制验证（仅验证 pipeline，不作最终结论）

---

## 10. 里程碑

| 里程碑 | 内容 | 依赖 | 预计产出 |
|--------|------|------|---------|
| M1 ✅ | ①②③ 实现并验证 | — | `check.ipynb`（已完成） |
| M2 | 阶段一/二增强（多句统计、死神经元、有效秩） | M1 | 稳定体检表 |
| M3 | 阶段三增强（≥50 句 + CKA + 放大倍数） | M2 | 微调影响报告 |
| M4 | 阶段四探针 + 注意力 + CKA | M3 + 数据 | 层级分工结论 |
| M5 | 阶段五分层头 + early-exit | M4 + 数据 | 精度-延迟曲线 |
| M6 | 汇总成最终报告，保存产物 | M5 | `report.md` + 模型文件 |

---

## 11. 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| backbone 与底座几乎相同（已确认） | ③ 的意义有限 | 把重心转向 ④⑤；③ 的结论如实记录 |
| 底座 `chinese-roberta-wwm-ext` ≠ 微调真正用到的底座 `chinese-wwm-roberta` | 对比严格性打折 | 如能找到真正底座则替换；找不到则注明为"近似对比" |
| 直连 huggingface.co 不通 | 数据/模型下载失败 | 一律走 `HF_ENDPOINT=https://hf-mirror.com` 或本地已有文件 |
| 无标签数据 | ④⑤ 无法训练 | 用公开中文数据集或自打标小集（见 §9） |
| 训练显存不足 | 联合训练 OOM | 冻结 backbone、小 batch、必要时只用 12 层头（不含 embedding） |
| 探针与分层头结论矛盾 | 方法学可信度 | 交叉验证：探针曲线应与分层头曲线形状一致，偏差大时排查数据/实现 |
| 4 位小数精度陷阱 | 权重对比失真 | 一律用 ≥6 位小数 + `mean_abs_diff` / `rel_l2` |

---

## 12. 最终交付

- [ ] `check.ipynb`：①②③ 完整可复跑
- [ ] `analysis/` 阶段四产物：探针曲线、注意力分工表、CKA 热图
- [ ] `earlyexit/` 阶段五产物：分层头模型文件、精度-延迟曲线
- [ ] `report.md`：汇总五项结论 + 复现说明（固定 seed、环境、数据）
