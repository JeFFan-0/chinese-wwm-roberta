# 推荐使用 conda 环境 nlp_fjq（torch 2.5.1 / transformers 5.15.0 / CUDA A100）
# 安装: conda activate nlp_fjq
torch==2.5.1
transformers==5.15.0
numpy>=1.24
pandas>=2.0
pyarrow>=14.0      # Parquet 缓存
PyYAML>=6.0
pytest>=7.0
matplotlib>=3.7    # 基准图表
